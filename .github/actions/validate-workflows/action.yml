name: Validate GitHub Actions workflows
description: Validate workflow syntax, embedded shell, and immutable action references.

runs:
  using: composite
  steps:
    - name: Install validators and check workflows
      shell: bash
      run: "${GITHUB_ACTION_PATH}/check.sh"
