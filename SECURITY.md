# Security policy

Report vulnerabilities privately through GitHub's security advisory interface. Do not open a public issue for credentials, infrastructure details, or exploitable workflow behavior.

The supported release line is v1. Calling workflows may use exact protected stable tags for public Pulse Actions entry points. Third-party actions must use full commit SHAs. Moving major tags such as `v1` are not supported.
